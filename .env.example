# Anthropic API key — get one at https://console.anthropic.com/
ANTHROPIC_API_KEY=your-api-key-here

# Enable experimental Agent Teams feature (required)
CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1

# Optional: override model (default is claude-opus-4-5 or latest)
# ANTHROPIC_MODEL=claude-opus-4-5

# Optional: use a third-party provider compatible with Anthropic API
# ANTHROPIC_BASE_URL=https://open.bigmodel.cn/api/anthropic
# ANTHROPIC_MODEL=GLM-5
# ─── Docker Sandbox (Execution Plane) ────────────────────────────────────────

# Set to 'docker' to run world tasks in Docker containers (vibeguild-sandbox image).
# Default is 'local' (in-process, same machine).
# RUNTIME_MODE=local

# GitHub personal access token with repo scope on the vibeguild org.
# Required in docker mode to auto-create a per-task GitHub repo for artifacts.
# VIBEGUILD_GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# GitHub org where task repos are created (default: vibeguild).
# VIBEGUILD_GITHUB_ORG=vibeguild

# Docker image name for sandbox containers.
# Build with: docker build -f Dockerfile.sandbox -t vibeguild-sandbox .
# SANDBOX_DOCKER_IMAGE=vibeguild-sandbox

# Absolute path to workspace root (auto-detected from process.cwd() if unset).
# WORKSPACE_ROOT=/absolute/path/to/vibeguild